#include <stdio.h>
int main()
{
	int n,a;
	scanf("%d", &n);
	int i,j;
	a = (n+1)/2;
	for (i = 1; i <= (n+1)/2; i++){
		for (j = 1; j < a;j++) {
			printf(".");
		}
		a = a - 1;
		for (j = 0; j < (i * 2) - 1; j++)
			printf("*");
		printf("\n");
	}
	return 0;
}
