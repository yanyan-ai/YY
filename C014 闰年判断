#include <stdio.h>
#include <math.h>
int main()
{
	int a, b,c;
	scanf("%d", &a);
	b = a % 100;
	if (b != 0) {
		c = a % 4;
	}
	else
		c = a/100 % 4;
	if (c == 0)
		printf("yes");
	else
		printf("no");
	return 0;
}
