#include <stdio.h>
int main()
{
	int x, y;
	int a, b, c, d;
	scanf("%d%d%d%d", &a, &b, &c, &d);
	scanf("%d%d", &x, &y);
	if (x <= c && x >= a && y >= b && y <= c)
		printf("true");
	else
		printf("false");
	return 0;
}
