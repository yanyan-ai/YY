#include <stdio.h>
#include <math.h>
int main()
{
	int sum;
	double a;
	char c;
	scanf("%lf %c", &a, &c);
	a = (a - 1000) / 500;
	a = ceil(a);
	if (c != 'y')
		sum = 12 + a * 5;
	else
		sum = 12 + 5 + a * 6;
	printf("%d", sum);
	return 0;
}
